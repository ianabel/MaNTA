
# Collect every test_*.py in this directory rather than naming one file, so new
# suites are picked up automatically. conftest.py fixes sys.path and cwd, so
# this also works as `pytest python/Tests` from the repo root.
PythonTests:
	pytest .

.PHONY: PythonTests
